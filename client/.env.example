# Dev: leave unset to use Vite proxy (/api → backend)
# VITE_API_URL=/api

# Production: full API origin if the app is hosted separately
# VITE_API_URL=https://api.example.edu

# Optional override for Socket.IO (defaults to current origin + /socket.io)
# VITE_SOCKET_URL=https://api.example.edu
